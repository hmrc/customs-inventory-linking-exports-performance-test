
# This file is read when runLocal = false in application.conf (default is false)

  services {

    auth {
      protocol = https
      host = customs-api-hods-stubs.protected.mdtp
      port = 80
    }

    inventory-linking-exports {
      protocol = http
      host = customs-inventory-linking-exports.protected.mdtp
      port = 80
    }

  }
