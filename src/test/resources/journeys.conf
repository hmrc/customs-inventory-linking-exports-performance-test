journeys {

  inventory-linking-exports-csp-movement {
    description = "Submit Csp 1.0 exports movement"
    load = 20
    feeder = data/test-account.csv
    parts = [
      submit-Csp-1-exports-movement
    ]
  }

  inventory-linking-exports-noncsp-movement {
    description = "Submit NonCsp 1.0 exports movement"
    load = 20
    feeder = data/test-account.csv
    parts = [
      submit-NonCsp-1-exports-movement
    ]
  }

  inventory-linking-exports-csp-consolidation {
    description = "Submit Csp 1.0 exports consolidation"
    load = 20
    feeder = data/test-account.csv
    parts = [
      submit-Csp-1-exports-consolidation
    ]
  }

  inventory-linking-exports-noncsp-consolidation {
    description = "Submit NonCsp 1.0 exports consolidation"
    load = 20
    feeder = data/test-account.csv
    parts = [
      submit-NonCsp-1-exports-consolidation
    ]
  }

  inventory-linking-exports-csp-query {
    description = "Submit Csp 1.0 exports query"
    load = 10
    feeder = data/test-account.csv
    parts = [
      submit-Csp-1-exports-query
    ]
  }

  inventory-linking-exports-noncsp-query {
    description = "Submit NonCsp 1.0 exports query"
    load = 10
    feeder = data/test-account.csv
    parts = [
      submit-NonCsp-1-exports-query
    ]
  }



}
